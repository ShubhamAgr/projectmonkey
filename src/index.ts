import { Logger } from './utils/logger';
import { config } from './utils/config';

Logger.info('Trading AI System starting up...');
Logger.info('Loaded config:', config);

// TODO: Initialize pools, managers, and start main event loop
